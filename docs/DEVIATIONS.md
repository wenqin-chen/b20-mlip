# Deviations from SPEC v1.0 / CONTRACTS v1.0 (running log)

Recorded as they are discovered during the build; the SPEC and CONTRACTS files are frozen at v1.0 and are
not edited retroactively.

| Date | Item | SPEC/CONTRACTS said | Reality | Consequence |
|---|---|---|---|---|
| 2026-09-17 | `[omat]` extra | `fairchem-core` for OMat24 ASE-LMDB | every fairchem-core release needs `e3nn>=0.5`; `mace-torch==0.3.16` pins `e3nn==0.4.4`, so uv cannot lock both | OMat24 `.aselmdb` shards are read natively (`lmdb` + `orjson`); `[omat]` = `lmdb`, `orjson` |
| 2026-09-17 | PySCF baseline | optional lower-fidelity reference (Si 16-atom, FeSi Γ) | 8-atom FeSi Γ spin-polarised KRKS did not finish in 15 min on the Mac (probe killed at 40 min) | `dft/pyscf_pbc.py` stays a guarded optional stage; never on the v0.1 path |
| 2026-09-17 | Local QE | none promised | no `pw.x` on macOS (conda-forge `qe` has no osx-arm64 build; no brew formula) | golden `pw.out` fixture is synthetic (labelled) until the first Tillicum output replaces it |
| 2026-09-18 | Tillicum partitions | "CPU nodes for QE, H200 GPU for replay/LAMMPS" | Tillicum is GPU-only: partitions `gpu-h200` and `gpu-h200-mig`; every job needs `--gpus=1` (8 CPUs + 200 GB per GPU) | QE arrays run on `gpu-h200-mig` (0.143× cost); templates emit `--gpus`, `--qos`; `partition_cpu` is the MIG partition by user choice at bootstrap |
| 2026-09-18 | Scratch layout | `/gscratch/<group>` | that is the Klone layout; Tillicum uses `/gpfs/projects/<group>` or `/gpfs/scrubbed/<user>` (60-day purge) | bootstrap probes both, never assumes |
| 2026-09-18 | `hyakalloc` | account/partition discovery | Klone tool, may be absent on Tillicum | `sacctmgr` + `sinfo` path is the default; raw output kept in the manifest |
| 2026-09-18 | Remote venv | `uv sync --frozen` on the cluster | `uv.lock` pins the CPU torch wheel on Linux | GPU replay jobs install a CUDA torch wheel inside the job (train tier), recorded in the manifest |
| 2026-09-18 | MPtrj B20 set | "60 B20 frames" (facts file, by-mp-id pull) | spglib-198 selector: 65 frames — the old pull included 13 frames of non-cubic FeGe mp-22510 (C2/m, excluded per SPEC §3) and missed CoGe mp-10692 (4) and MnGe mp-1078464 (14; a = 4.762 Å) | `data/frames/mptrj_b20.extxyz` (65) is the offset/keyless set; MnGe needs no OPTIMADE search |
| 2026-09-18 | OMat24 in-family | "few hundred expected" | 656 in-chemsys frames of 1,171,309 scanned; 477 after the 15 eV/Å force cap (179 dropped); 0 are spg-198 | bootstrap/T3 set = 477 frames, stress already ASE-convention (r = +0.965 vs MPA-0) |
| 2026-09-18 | WBM sample prevalence | 16.7 % (population) | 15.2 % realised for seed 0, N = 1000; in-family stratum N = 312, 1.28 % stable | discovery captions report the realised sample prevalence |
| 2026-09-18 | Round-0 candidates | 400–600 × 8-atom | 420 generated → 300 after StructureMatcher dedupe (rotation-equivalent strains/shears merged) | QE round-0 array = 300 + phonon/vacancy/noise-floor units; more rattle seeds if the budget allows |
| 2026-09-19 | QE launcher | `mpirun -np 8 pw.x -nk 2` (site MPI) | conda-forge QE on Tillicum: the default UCX transport (with the site's gcc/cuda/openmpi modules loaded) let CoSi SCFs "converge" to +2230 Ry with ~3×10³ e of negative density; the same input over shared memory (`--mca pml ob1 --mca btl self,sm`, no modules) matches the serial result to 1e-8 Ry and is 11× faster than 2 OpenMP threads | launcher = ob1/sm, QE jobs load no site modules; unit script + parser reject runs with negative rho > 0.1 e (healthy runs: ≤ 4×10⁻⁴ e); all four production references re-verified bit-for-bit |
| 2026-09-19 | LAMMPS build | libtorch 2.14 + `-std=c++17` | libtorch ≥ 2.9 headers require C++20; in-tree Kokkos 4 needs `Kokkos_ENABLE_IMPL_MDSPAN=OFF`; libtorch's caffe2::mkl needs an existing `MKL_INCLUDE_DIR` | built (10 Sep 2025 LAMMPS, ACEsuit `mace` 4d222cb, Kokkos+CUDA HOPPER90, `mace` and `mace/kk` pair styles) at `<scratch>/b20-mlip/bin/lmp` |
| 2026-09-19 | Production cutoff | "converge 1 h (E ≤ 1 meV/atom, F ≤ 5 meV/Å)" | four 24-point scans (~$1.5 total): MnSi meets both at 80 Ry / k 0.20; Fe compounds need 90 Ry (PAW Fe: 80 Ry is still 6.5–7 meV/atom above 90); forces need k ≤ 0.25 for MnSi | frozen `configs/dft/qe_b20.yaml`: 90/1080 Ry, k 0.20 Å⁻¹, MV 0.01 Ry, mixing 0.3; ≈ 4.5 min per 8-atom frame on one MIG slice |

| 2026-09-19 | Tillicum budget | 45–60 node-h of QE assumed available | per-user budget on `stf` is $250 with $138 used before the project; the scheduler bills *pending* jobs at full requested walltime and refused new submissions with a stale $529 "pending" figure after my arrays were cancelled; actual project spend so far ≈ $0.64 | QE walltimes cut to 25 min per unit (estimate ≈ $18 for round 0); 64-atom sets (≈ $35) on hold until the budget question is settled; a watcher retries submission every 15 min |
| 2026-09-19 | Round-0 walltime | "8-atom ≈ 3 min" | with the frozen 90 Ry / k 0.20 setting a low-symmetry 8-atom cell has 172–260 irreducible k-points: p50 6 min, p90 9 min, max > 12 min per unit on 2 ranks; a 12-min cap (chosen to shrink the budget reservation) timed out 179 of 265 units (≈ $4 lost) | cap = 45 min (≥ 4× p90); walltime caps are never set below 2× the observed p90 again |
| 2026-09-19 | Isolated-atom E0s | "converge 1 h" | spin-polarised Fe atom in a 12 Å box at 90/1080 Ry: 123 SCF iterations in 50 min without convergence at mixing 0.3 | Fe rerun with mixing_beta 0.1, electron_maxstep 400, 3 h cap; Co took 29 min (65 iterations), Mn 13 min |
| 2026-09-20 | Umbrella CV | hop CV = projection of the hopping atom's displacement (absolute frame) | under Langevin the biased crystal translated rigidly at zero cost: 24 GPU windows gave a flat potential-energy profile and a 0.11 eV "barrier" against a 0.69 eV NEB barrier (run 20260919T184957 kept as a negative result, numbers withheld) | CV measured in the frame of the other atoms' centre (gradient sums to zero); rerun with the corrected CV |
| 2026-09-20 | Round-0 split | `sha256(group_id|seed)` 80/10/10 of groups | only 26 groups (5 compounds × ≤ 6 config types; all rattles of a parent share a group): seed 0 held out 31 % of the trainable frames, seed 1 only 6 %; seed 3 (train 161 / val 29 / held-out ≈ 17 % of FeSi+CoSi+MnSi frames) chosen as the realisation closest to 80/10/10 among seeds 0–5, before any model saw the data | split `data/splits/<id>.json` (seed 3); T2 = 115 FeGe + MnGe frames never trained; 11 frames removed by the magnetic-branch filter (MnSi 4, FeGe 3, MnGe 4: strain ±6 % moments 0–2.6 μB vs refs 1.00/1.05/2.04) |
| 2026-09-21 | B2 replay size | "10k FPS MPtrj, 20 epochs" | 10,000 replay frames against 161 QE frames per epoch drowned the QE head (val force RMSE 122 → 135 meV/Å, exported epoch 0); the SPEC's own fallback ratio, 2,000 replay frames (the first 2,000 of the FPS-10k selection), trains normally (exported epoch 5) and is better on every tier (T0 57.0 vs 59.1, T2 70.5 vs 72.7, T4a 25.2 vs 25.7 meV/Å) | B2 = 2k replay, 3 seeds; the 10k run stays in provenance as a single-seed variant; MACE's built-in FPS path recomputes 146k descriptors on the CPU, so the subset is pre-selected once on a GPU and passed as a file |
| 2026-09-22 | Split policy (T0, val) | CONTRACTS: "T0 = val + test frames labelled by QE (held-out groups)"; group-level 80/10/10 | with ~20 trainable groups the legacy `group_hash` split `d195dc5f2104` put a held-out-compound group (29 MnGe rattles) into **val** (used for MACE checkpoint selection and the learning-rate sweep) and made T0 = 147 frames of which 115 were T2 (FeGe/MnGe); only 32 T0 frames were held-out groups of trained compounds. The README T0 row (B0 65.6, B1 65.5, B2 57.0 meV/Å) and the resume phrase "held-out force error 13 % (65.5→57)" describe that mixed tier | new policy `group_hash_v2`: non-trainable frames (hot, held-out compounds) always test; val = frame-level 1/9 of trainable frames in non-held-out groups; T0 = held-out groups of trained compounds only. v2 split `006f595381e8` (457 frames incl. 149 hot-MD): train 193, val 28, T0 32, T1 49, T2 175. On the clean T0, the v1 models give B0 24.2 → B1 7.5 (6.8–8.5) / B2 9.2 (8.4–10.9) meV/Å; all brackets retrained and re-evaluated on v2 |
| 2026-09-22 | B1 learning rate | sweep {1e-4, 3e-4, 1e-3} on seed 0 → 1e-4 | the v1 sweep selected on MnGe val frames; the v2 sweep (val = trained compounds) gives val force RMSE 41.8 / 39.0 / 42.9 meV/Å → **3e-4** | B1 v2 uses 3e-4; B2 keeps the SPEC's 1e-4 |
| 2026-09-22 | LAMMPS parity | 20 frames, max\|ΔF\| < 1e-3 eV/Å, \|ΔE\| < 1e-4 eV/atom | LAMMPS 10 Sep 2025 (ACEsuit `mace` branch), Kokkos 4.6.2 on one H200, `pair_style mace no_domain_decomposition`, MPA-0 exported with `mace_create_lammps_model`, 20 64-atom frames across 5 compounds vs ASE on the Mac CPU: max\|ΔF\| 4.0e-14 eV/Å, max\|ΔE\| 8.0e-15 eV/atom | gate passed; the export CLI had a relative-path bug (fixed) |
| 2026-09-22 | T4a "held-out MPtrj frames" | T4a = forces on held-out MPtrj in-family frames (forgetting) | 28 of the 65 MPtrj B20 frames (27 geometries: 26 relaxed parents, one EOS point) share their geometry with QE-relabelled frames of the offset map that sit in the v2 train split, and two more with val; three geometries appear twice or three times in MPtrj with different energies (different MP calculations) | T4a is kept as a forgetting metric (distance from the MP VASP labels), the README says so explicitly; a tier restricted to never-trained geometries is in the Plan |
| 2026-09-22 | Frame ids through extxyz | `frame_id = sha256(numbers, positions, cell)[:16]` string | ASE 3.29's extxyz reader converts numeric-looking unquoted values: an id such as `3e71…` becomes `inf`, `0123…` an int without the leading zero. One QE frame (and its unit id) reads as `inf` in `labelled_r0plus`; split and eval see the same `inf`, and `group_split` rejects duplicate ids, so no number changed | fix (string-safe read, legacy alias for the published v2 split) tracked separately |
| 2026-09-22 | README captions | every README number generated from `numbers.json` | four captions carried hand-typed facts that had drifted: WBM prevalence "16.7 %" (realised 15.2 %), thermal-expansion "64-atom cells" (the LAMMPS row is 512 atoms), umbrella "12 windows" (24 were run), and the sampling table compared the end-to-end ΔF (≈ 0 for a symmetric hop) with the NEB barrier | captions are now generated from run meta; the sampling table and the summary line compare the umbrella barrier ΔF‡ with NEB; T2 is labelled FeGe + MnGe; zero-shot energy cells read n/a (MP scale) |
| 2026-09-22 | Agent eval, first live run | 12 tasks scored against `screen` gold | run `20260922T174732-26ca37-0` (Claude Opus 5, $1.08): accuracy 0.889; three misses were eval defects (t05 gold committee included the zero-shot model; t06 graded a key the prompt never asked for; t03 prompt did not fix the phonon cell) and the injected failures of t09/t11 were counted as the agent's invalid calls | defects fixed and documented in `evals/README.md` before a second live run, which is the published one; the first run stays in `runs/` |
| 2026-09-22 | CI after the task revision | green on f9abc1b | the t04/t05 revision in c0bb18b pointed the tasks at `labelled_r0plus` / split `v2`, which the agent test fixture did not provide, and `scripts/force_sets_from_sampled.py` failed `ruff format` | fixture builds `labelled_r0plus` and a `v2` split; script formatted |
